Main API (requires auth):

- /api/routes, /api/stops, /api/vehicles, /api/arrivals,
  /api/schedule, /api/feed

Timetable API (requires auth):

- /timetable/api/routes, /timetable/api/stops,
  /timetable/api/trips, /timetable/api/lines
- /timetable/api/agencies, /timetable/api/calendar,
  /timetable/api/stop_times
- /timetable/api/shapes, /timetable/api/feed_info
